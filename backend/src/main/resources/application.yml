spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/taskapp
    username: taskapp
    password: taskapp_dev
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

app:
  jwt:
    secret: "ThisIsADevelopmentSecretKeyThatShouldBeAtLeast256BitsLong!!"
    expiration-ms: 86400000

server:
  port: 8080
