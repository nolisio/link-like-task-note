spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/link_like_task_note
    username: link_like_task_note
    password: link_like_task_note_dev
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

app:
  jwt:
    secret: "ThisIsADevelopmentSecretKeyThatShouldBeAtLeast256BitsLong!!"
    expiration-ms: 86400000

server:
  port: 8080
